<% provide(:title, 'Edit landlord') %>
<h1>Update landlord</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@landlord) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Save changes", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>  
